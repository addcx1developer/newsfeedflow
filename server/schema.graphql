# @generated
# run `yarn print-schema` to re-generate this file.

type Person implements Actor {
  id: ID!
  name: String
  profilePicture: Image
  joined: String
  email: String
  location: Location
}

interface Actor {
  id: ID!
  name: String
  profilePicture: Image
  joined: String
}

type Image {
  url: String!
  altText: String
}

type Location {
  id: ID!
  name: String!
}

type Query {
  viewer: Viewer
}

type Viewer {
  actor: Actor
}
